[project]
name = "skill-sftp-file-manager"
version = "0.1.0"
description = "Manage NAS files over SFTP using Python only"
readme = "SKILL.md"
requires-python = ">=3.10"
dependencies = [
  "paramiko>=3.4.0",
  "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
